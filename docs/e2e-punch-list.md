# E2E Punch List

**Status:** Open — items added from live walkthrough, batched AFTER Block C completes.
**Started:** 2026-08-05

---

## P1. Brief modal header density

Adopt the Plan/DG header pattern (compact slug line + one-sentence purpose). Compress approver to one quiet line. Shrink the no-discovery warning to one sentence: "No discovery yet — run /qori-discover first, or proceed with your inputs alone." The Brief predates the cleaner pattern; it conforms to it now, not vice versa.

## P2. Discussion Guide pre-fill jargon (R5)

Pre-filled objectives and research questions render internal IDs ("OBJ-001", "RQ-001 (Primary)") in researcher-facing editable content. IDs stay in the cascade; pre-fill renders clean text.

**Investigation needed:** The objectives and RQ pre-fills show the SAME sentence duplicated across both fields. Report whether that's the cascade storing one item in both variables or the pre-fill mapper double-reading.

## P3. New §4 ruling: routing selectors first

Routing selectors (Study/Session) are always the FIRST interactive element in a modal; mode tabs and inputs follow. One modal moves: Session Notes (Select Session above the Manual Notes / Upload Transcript tabs). Add to design standard with a ruling number.

## P4. Model-selection audit (pre-launch worthy)

For every AI task across all templates, report: task → current model → task character (synthesis/extraction on the provenance chain vs. structured drafting vs. trivial transform) → recommended model tier, with rationale. Constraint: anything with anti-fabrication grounding rules or provenance-chain output stays on the strongest tier — cost optimization never trades against the fabrication guards. Include rough per-task token volumes so recommendations carry cost impact. Recommendations only; Lapedra rules per row.

---

*This list stays open. More items will be added as the walkthrough continues.*
