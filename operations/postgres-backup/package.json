{
  "name": "qori-postgres-backup",
  "version": "1.0.0",
  "private": true,
  "description": "Scheduled PostgreSQL logical backup to offsite S3-compatible storage",
  "main": "backup.js",
  "scripts": {
    "backup": "node backup.js",
    "test": "jest --config jest.config.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
