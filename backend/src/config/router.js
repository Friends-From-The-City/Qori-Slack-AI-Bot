// router.js - Route configuration
// Note: All routes are now mounted directly in app.js
// This file is preserved for potential future REST API routes

module.exports = function (app) {
  // No routes currently - Slack commands handled by Bolt app
  // GitHub webhook mounted directly in app.js before express.json()
};
